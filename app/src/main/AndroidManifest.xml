<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.geodrone">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:name="android.hardware.camera2.full" />

    <!--
    <uses-feature android:name="android.hardware.location.gps" />
    <uses-feature android:name="android.permission.CALL_PHONE" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    -->
    <application
        android:name=".GeoDroneApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activity.SplashScreenActivity"
            android:theme="@style/AppTheme.TelaCheia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.registrochuva.RegistroPluviosidadeActivity"
            android:label="@string/title_activity_reg_chuvas"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.usuario.CadatroUsuarioActivity"
            android:label="@string/title_activity_cadatro_usuario"
            android:parentActivityName=".ui.login.LoginActivity" />
        <activity
            android:name=".ui.registroimagem.CadastroImagemActivity"
            android:label="@string/title_activity_cadastro_imagem" />
        <activity android:name=".ui.registroimagem.CameraFotoActivity" />
        <activity
            android:name=".ui.registropraga.RegistroPragaActivity"
            android:label="@string/title_activity_registro_pragas"
            android:parentActivityName=".ui.main.MainActivity" />
        <activity
            android:name=".activity.MensagemActivity"
            android:parentActivityName=".ui.main.MainActivity" />
        <activity
            android:name=".activity.ForunActivity"
            android:parentActivityName=".ui.main.MainActivity" />
        <activity
            android:name=".ui.monitoramento.MonitoramentoActivity"
            android:label="@string/title_activity_monitoramento"
            android:parentActivityName=".ui.main.MainActivity" />
        <activity android:name=".activity.ConsultarPragaActivity" />
        <activity android:name=".ui.registrodoenca.RegistroDoencaActivity"></activity>
    </application>

</manifest>